require 'spec_helper'

describe Identicon do
  it 'has a version number' do
    expect(Identicon::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
